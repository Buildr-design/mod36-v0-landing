// src/components/sections/how-it-works-section.tsx
// This component is no longer used in the new "Mod36 v0" page structure.
// For now, I'll leave it empty to avoid build errors if it's still imported somewhere unexpectedly.
'use client';

export function HowItWorksSection({ content }: { content?: any }) { // Use `any` for content type as it's deprecated
  return null;
}
    
